<Page
    x:Class="SUNWODA_SEVB.Views.Pages.Common.UserCenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:SUNWODA_SEVB.Views.Pages.Common"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="UserCenter"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <TextBlock
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="50"
            Text="用户中心" />
    </Grid>

    <!--<Page.Resources>
    -->
    <!--  定义样式资源  -->
    <!--
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,20,0,30" />
        </Style>

    -->
    <!--  权限等级颜色  -->
    <!--
        <SolidColorBrush x:Key="SuperAdminBrush" Color="#E91E63" />
        <SolidColorBrush x:Key="AdminBrush" Color="#2196F3" />
        <SolidColorBrush x:Key="EngineerBrush" Color="#4CAF50" />
        <SolidColorBrush x:Key="GuestBrush" Color="#9E9E9E" />

    -->
    <!--  数据转换器  -->
    <!--
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </Page.Resources>

    <Grid>
    -->
    <!--  背景渐变  -->
    <!--
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Offset="0" Color="#F5F7FA" />
                <GradientStop Offset="1" Color="#E8EAF0" />
            </LinearGradientBrush>
        </Grid.Background>

    -->
    <!--  主容器  -->
    <!--
        <Border
            Margin="20"
            Background="White"
            CornerRadius="10"
            Effect="{DynamicResource EffectShadow2}">
            <Grid>
    -->
    <!--  登录界面  -->
    <!--
                <Grid x:Name="LoginPanel" Visibility="{Binding IsNotLoggedIn, Converter={StaticResource BoolToVisibility}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

    -->
    <!--  标题区域  -->
    <!--
                    <StackPanel Grid.Row="0">
                        <TextBlock Style="{StaticResource HeaderTextStyle}" Text="用户登录" />
                        <hc:Divider Margin="50,0" LineStrokeThickness="1" />
                    </StackPanel>

    -->
    <!--  登录表单  -->
    <!--
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel
                            MaxWidth="400"
                            Margin="0,40,0,20"
                            HorizontalAlignment="Center">
    -->
    <!--  Logo或图标  -->
    <!--
                            <Border
                                Width="80"
                                Height="80"
                                Margin="0,0,0,30"
                                HorizontalAlignment="Center"
                                Background="{DynamicResource PrimaryBrush}"
                                CornerRadius="40">
                                <Path
                                    Width="40"
                                    Height="40"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Data="{StaticResource AddGeometry}"
                                    Fill="White"
                                    Stretch="Uniform" />
                            </Border>

    -->
    <!--  用户名输入  -->
    <!--
                            <hc:TextBox
                                x:Name="UsernameTextBox"
                                Margin="0,10"
                                hc:InfoElement.Placeholder="请输入用户名"
                                hc:InfoElement.ShowClearButton="True"
                                hc:InfoElement.Title="用户名"
                                Style="{StaticResource TextBoxExtend}"
                                Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}">
                                <hc:TextBox.InputBindings>
                                    <KeyBinding Key="Enter" Command="{Binding LoginCommand}" />
                                </hc:TextBox.InputBindings>
                            </hc:TextBox>

    -->
    <!--  密码输入  -->
    <!--
                            <hc:PasswordBox
                                x:Name="PasswordBox"
                                Margin="0,10"
                                hc:InfoElement.Placeholder="请输入密码"
                                hc:InfoElement.Title="密码"
                                IsSafeEnabled="False"
                                ShowEyeButton="True">
                                <hc:PasswordBox.InputBindings>
                                    <KeyBinding Key="Enter" Command="{Binding LoginCommand}" />
                                </hc:PasswordBox.InputBindings>
                            </hc:PasswordBox>

    -->
    <!--  记住密码  -->
    <!--
                            <CheckBox
                                Margin="0,15,0,0"
                                Content="记住密码"
                                IsChecked="{Binding RememberPassword}" />

    -->
    <!--  登录按钮  -->
    <!--
                            <Button
                                Height="40"
                                Margin="0,25,0,10"
                                Command="{Binding LoginCommand}"
                                Content="登录"
                                Style="{StaticResource ButtonPrimary}" />

    -->
    <!--  访客模式  -->
    <!--
                            <TextBlock Margin="0,10" HorizontalAlignment="Center">
                                <Run Text="或者" />
                                <Hyperlink Command="{Binding GuestModeCommand}">
                                    <Run Foreground="{DynamicResource PrimaryBrush}" Text="以访客身份继续" />
                                </Hyperlink>
                            </TextBlock>

    -->
    <!--  错误提示  -->
    <!--
                            <Border
                                Margin="0,15,0,0"
                                Padding="10"
                                Background="#FFF3E0"
                                BorderBrush="#FFB74D"
                                BorderThickness="1"
                                CornerRadius="4"
                                Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock
                                    Foreground="#E65100"
                                    Text="{Binding ErrorMessage}"
                                    TextWrapping="Wrap" />
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

    -->
    <!--  底部信息  -->
    <!--
                    <StackPanel Grid.Row="2" Margin="0,0,0,20">
                        <hc:Divider Margin="50,0" LineStrokeThickness="1" />
                        <TextBlock
                            Margin="0,10,0,0"
                            HorizontalAlignment="Center"
                            Foreground="{StaticResource GuestBrush}"
                            Text="当前状态：未登录 (访客权限)" />
                    </StackPanel>
                </Grid>

    -->
    <!--  用户中心界面（登录后显示）  -->
    <!--<Grid x:Name="UserCenterPanel" Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVisibility}}">-->
    <!--
                <Grid x:Name="UserCenterPanel" Visibility="Hidden">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

    -->
    <!--  顶部用户信息栏  -->
    <!--
                    <Border
                        Grid.Row="0"
                        Background="{DynamicResource RegionBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,0,0,1">
                        <Grid Margin="20,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

    -->
    <!--  用户头像  -->
    <!--
                            <Border
                                Grid.Column="0"
                                Width="50"
                                Height="50"
                                Margin="0,0,15,0"
                                Background="{Binding RoleBrush}"
                                CornerRadius="25">
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="20"
                                    FontWeight="Bold"
                                    Foreground="White"
                                    Text="{Binding UserInitial}" />
                            </Border>

    -->
    <!--  用户信息  -->
    <!--
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock FontSize="18" FontWeight="SemiBold">
                                    <Run Text="欢迎，" />
                                    <Run Text="{Binding CurrentUser.Username}" />
                                </TextBlock>
                                <TextBlock Margin="0,2,0,0">
                                    <Run Foreground="Gray" Text="权限等级：" />
                                    <Run
                                        FontWeight="SemiBold"
                                        Foreground="{Binding RoleBrush}"
                                        Text="{Binding CurrentUser.RoleDisplay}" />
                                </TextBlock>
                            </StackPanel>

    -->
    <!--  操作按钮  -->
    <!--
                            <StackPanel
                                Grid.Column="2"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <Button
                                    Margin="0,0,10,0"
                                    Command="{Binding ChangePasswordCommand}"
                                    Content="修改密码"
                                    Style="{StaticResource ButtonInfo}" />
                                <Button
                                    Command="{Binding LogoutCommand}"
                                    Content="退出登录"
                                    Style="{StaticResource ButtonDanger}" />
                            </StackPanel>
                        </Grid>
                    </Border>

    -->
    <!--  主内容区域  -->
    <!--
                    <TabControl
                        Grid.Row="1"
                        Margin="20"
                        Style="{StaticResource TabControlInLine}">
    -->
    <!--  个人信息标签  -->
    <!--
                        <TabItem Header="个人信息">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel Margin="20">
                                    <hc:Card Margin="0,0,0,15">
                                        <Grid Margin="20">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="120" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="35" />
                                                <RowDefinition Height="35" />
                                                <RowDefinition Height="35" />
                                                <RowDefinition Height="35" />
                                                <RowDefinition Height="35" />
                                            </Grid.RowDefinitions>

                                            <TextBlock
                                                Grid.Row="0"
                                                Grid.Column="0"
                                                VerticalAlignment="Center"
                                                Text="用户ID：" />
                                            <TextBlock
                                                Grid.Row="0"
                                                Grid.Column="1"
                                                VerticalAlignment="Center"
                                                Text="{Binding CurrentUser.UserId}" />

                                            <TextBlock
                                                Grid.Row="1"
                                                Grid.Column="0"
                                                VerticalAlignment="Center"
                                                Text="用户名：" />
                                            <TextBlock
                                                Grid.Row="1"
                                                Grid.Column="1"
                                                VerticalAlignment="Center"
                                                Text="{Binding CurrentUser.Username}" />

                                            <TextBlock
                                                Grid.Row="2"
                                                Grid.Column="0"
                                                VerticalAlignment="Center"
                                                Text="权限角色：" />
                                            <Border
                                                Grid.Row="2"
                                                Grid.Column="1"
                                                Padding="8,2"
                                                HorizontalAlignment="Left"
                                                Background="{Binding RoleBrush}"
                                                CornerRadius="3">
                                                <TextBlock
                                                    FontWeight="SemiBold"
                                                    Foreground="White"
                                                    Text="{Binding CurrentUser.RoleDisplay}" />
                                            </Border>

                                            <TextBlock
                                                Grid.Row="3"
                                                Grid.Column="0"
                                                VerticalAlignment="Center"
                                                Text="创建时间：" />
                                            <TextBlock
                                                Grid.Row="3"
                                                Grid.Column="1"
                                                VerticalAlignment="Center"
                                                Text="{Binding CurrentUser.CreatedDate}" />

                                            <TextBlock
                                                Grid.Row="4"
                                                Grid.Column="0"
                                                VerticalAlignment="Center"
                                                Text="最后登录：" />
                                            <TextBlock
                                                Grid.Row="4"
                                                Grid.Column="1"
                                                VerticalAlignment="Center"
                                                Text="{Binding CurrentUser.LastLoginDate}" />
                                        </Grid>
                                    </hc:Card>

    -->
    <!--  权限说明  -->
    <!--
                                    <hc:Card>
                                        <StackPanel Margin="20">
                                            <TextBlock
                                                Margin="0,0,0,15"
                                                FontSize="16"
                                                FontWeight="SemiBold"
                                                Text="当前权限说明" />
                                            <ItemsControl ItemsSource="{Binding CurrentPermissions}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Margin="0,5">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="20" />
                                                                <ColumnDefinition Width="*" />
                                                            </Grid.ColumnDefinitions>
                                                            <Path
                                                                Grid.Column="0"
                                                                Width="16"
                                                                Height="16"
                                                                Data="{StaticResource SuccessGeometry}"
                                                                Fill="#4CAF50" />
                                                            <TextBlock
                                                                Grid.Column="1"
                                                                Margin="5,0,0,0"
                                                                Text="{Binding}" />
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </hc:Card>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>

    -->
    <!--  用户管理标签（仅管理员可见）  -->
    <!--
                        <TabItem Header="用户管理" Visibility="{Binding CanManageUsers, Converter={StaticResource BoolToVisibility}}">
                            <Grid Margin="20">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

    -->
    <!--  工具栏  -->
    <!--
                                <ToolBarTray Grid.Row="0">
                                    <ToolBar>
                                        <Button
                                            hc:IconElement.Geometry="{StaticResource AddGeometry}"
                                            Command="{Binding AddUserCommand}"
                                            Content="新增用户" />
                                        <Separator />
                                        <Button
                                            hc:IconElement.Geometry="{StaticResource PageModeGeometry}"
                                            Command="{Binding EditUserCommand}"
                                            Content="编辑" />
                                        <Button
                                            hc:IconElement.Geometry="{StaticResource DeleteGeometry}"
                                            Command="{Binding DeleteUserCommand}"
                                            Content="删除" />
                                        <Separator />
                                        <Button
                                            hc:IconElement.Geometry="{StaticResource RotateRightGeometry}"
                                            Command="{Binding RefreshUsersCommand}"
                                            Content="刷新" />
                                    </ToolBar>
                                </ToolBarTray>

    -->
    <!--  用户列表  -->
    <!--
                                <DataGrid
                                    Grid.Row="1"
                                    Margin="0,10,0,0"
                                    AutoGenerateColumns="False"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CanUserAddRows="False"
                                    GridLinesVisibility="Horizontal"
                                    HeadersVisibility="All"
                                    ItemsSource="{Binding UserList}"
                                    SelectedItem="{Binding SelectedUser}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn
                                            Width="60"
                                            Binding="{Binding UserId}"
                                            Header="ID" />
                                        <DataGridTextColumn
                                            Width="120"
                                            Binding="{Binding Username}"
                                            Header="用户名" />

                                        <DataGridTemplateColumn Width="120" Header="权限角色">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Border
                                                        Margin="2"
                                                        Padding="6,2"
                                                        Background="{Binding RoleBrush}"
                                                        CornerRadius="3">
                                                        <TextBlock
                                                            FontSize="12"
                                                            Foreground="White"
                                                            Text="{Binding RoleDisplay}" />
                                                    </Border>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <DataGridTextColumn
                                            Width="150"
                                            Binding="{Binding CreatedDate}"
                                            Header="创建时间" />
                                        <DataGridTextColumn
                                            Width="150"
                                            Binding="{Binding LastLoginDate}"
                                            Header="最后登录" />

                                        <DataGridTemplateColumn Width="80" Header="状态">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <hc:Badge Style="{Binding StatusBadgeStyle}" Text="{Binding StatusText}" />
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <DataGridTemplateColumn Width="150" Header="操作">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <Button
                                                            Margin="2"
                                                            Command="{Binding DataContext.EditUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                            CommandParameter="{Binding}"
                                                            Content="编辑"
                                                            Style="{StaticResource ButtonInfo.Small}" />
                                                        <Button
                                                            Margin="2"
                                                            Command="{Binding DataContext.ResetPasswordCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                            CommandParameter="{Binding}"
                                                            Content="重置密码"
                                                            Style="{StaticResource ButtonWarning.Small}" />
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </TabItem>

    -->
    <!--  权限设置标签（仅超级管理员可见）  -->
    <!--
                        <TabItem Header="权限设置" Visibility="{Binding IsSuperAdmin, Converter={StaticResource BoolToVisibility}}">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel Margin="20">
    -->
    <!--  权限等级说明  -->
    <!--
                                    <hc:Card Margin="0,0,0,20">
                                        <StackPanel Margin="20">
                                            <TextBlock
                                                Margin="0,0,0,15"
                                                FontSize="18"
                                                FontWeight="Bold"
                                                Text="系统权限等级" />

    -->
    <!--  超级管理员  -->
    <!--
                                            <Border
                                                Margin="0,0,0,10"
                                                Padding="15"
                                                Background="#FFF3E0"
                                                CornerRadius="5">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="150" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                                        <Border
                                                            Padding="8,3"
                                                            Background="{StaticResource SuperAdminBrush}"
                                                            CornerRadius="3">
                                                            <TextBlock
                                                                FontWeight="Bold"
                                                                Foreground="White"
                                                                Text="超级管理员" />
                                                        </Border>
                                                    </StackPanel>
                                                    <TextBlock
                                                        Grid.Column="1"
                                                        VerticalAlignment="Center"
                                                        TextWrapping="Wrap">
                                                        拥有系统所有权限，可管理所有用户和设置
                                                    </TextBlock>
                                                </Grid>
                                            </Border>

    -->
    <!--  管理员  -->
    <!--
                                            <Border
                                                Margin="0,0,0,10"
                                                Padding="15"
                                                Background="#E3F2FD"
                                                CornerRadius="5">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="150" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                                        <Border
                                                            Padding="8,3"
                                                            Background="{StaticResource AdminBrush}"
                                                            CornerRadius="3">
                                                            <TextBlock
                                                                FontWeight="Bold"
                                                                Foreground="White"
                                                                Text="管理员" />
                                                        </Border>
                                                    </StackPanel>
                                                    <TextBlock
                                                        Grid.Column="1"
                                                        VerticalAlignment="Center"
                                                        TextWrapping="Wrap">
                                                        可管理普通用户，修改系统配置，查看所有数据
                                                    </TextBlock>
                                                </Grid>
                                            </Border>

    -->
    <!--  工程师  -->
    <!--
                                            <Border
                                                Margin="0,0,0,10"
                                                Padding="15"
                                                Background="#E8F5E9"
                                                CornerRadius="5">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="150" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                                        <Border
                                                            Padding="8,3"
                                                            Background="{StaticResource EngineerBrush}"
                                                            CornerRadius="3">
                                                            <TextBlock
                                                                FontWeight="Bold"
                                                                Foreground="White"
                                                                Text="工程师" />
                                                        </Border>
                                                    </StackPanel>
                                                    <TextBlock
                                                        Grid.Column="1"
                                                        VerticalAlignment="Center"
                                                        TextWrapping="Wrap">
                                                        可执行技术操作，修改部分配置，查看相关数据
                                                    </TextBlock>
                                                </Grid>
                                            </Border>

    -->
    <!--  访客  -->
    <!--
                                            <Border
                                                Padding="15"
                                                Background="#F5F5F5"
                                                CornerRadius="5">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="150" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                                        <Border
                                                            Padding="8,3"
                                                            Background="{StaticResource GuestBrush}"
                                                            CornerRadius="3">
                                                            <TextBlock
                                                                FontWeight="Bold"
                                                                Foreground="White"
                                                                Text="访客" />
                                                        </Border>
                                                    </StackPanel>
                                                    <TextBlock
                                                        Grid.Column="1"
                                                        VerticalAlignment="Center"
                                                        TextWrapping="Wrap">
                                                        仅可查看公开数据，无修改权限
                                                    </TextBlock>
                                                </Grid>
                                            </Border>
                                        </StackPanel>
                                    </hc:Card>

    -->
    <!--  权限矩阵  -->
    <!--
                                    <hc:Card>
                                        <StackPanel Margin="20">
                                            <TextBlock
                                                Margin="0,0,0,15"
                                                FontSize="18"
                                                FontWeight="Bold"
                                                Text="功能权限矩阵" />
                                            <DataGrid
                                                AutoGenerateColumns="False"
                                                CanUserAddRows="False"
                                                HeadersVisibility="All"
                                                ItemsSource="{Binding PermissionMatrix}">
                                                <DataGrid.Columns>
                                                    <DataGridTextColumn
                                                        Width="150"
                                                        Binding="{Binding ModuleName}"
                                                        Header="功能模块" />
                                                    <DataGridCheckBoxColumn
                                                        Width="100"
                                                        Binding="{Binding SuperAdmin}"
                                                        Header="超级管理员" />
                                                    <DataGridCheckBoxColumn
                                                        Width="80"
                                                        Binding="{Binding Admin}"
                                                        Header="管理员" />
                                                    <DataGridCheckBoxColumn
                                                        Width="80"
                                                        Binding="{Binding Engineer}"
                                                        Header="工程师" />
                                                    <DataGridCheckBoxColumn
                                                        Width="80"
                                                        Binding="{Binding Guest}"
                                                        Header="访客" />
                                                </DataGrid.Columns>
                                            </DataGrid>
                                        </StackPanel>
                                    </hc:Card>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>

    -->
    <!--  操作日志标签  -->
    <!--
                        <TabItem Header="操作日志" Visibility="{Binding CanViewLogs, Converter={StaticResource BoolToVisibility}}">
                            <Grid Margin="20">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

    -->
    <!--  筛选条件  -->
    <!--
                                <hc:Card Grid.Row="0" Margin="0,0,0,10">
                                    <Grid Margin="15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="200" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="200" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock
                                            Grid.Column="0"
                                            Margin="0,0,10,0"
                                            VerticalAlignment="Center"
                                            Text="开始日期：" />
                                        <DatePicker Grid.Column="1" SelectedDate="{Binding LogStartDate}" />

                                        <TextBlock
                                            Grid.Column="2"
                                            Margin="20,0,10,0"
                                            VerticalAlignment="Center"
                                            Text="结束日期：" />
                                        <DatePicker Grid.Column="3" SelectedDate="{Binding LogEndDate}" />

                                        <Button
                                            Grid.Column="5"
                                            Command="{Binding SearchLogsCommand}"
                                            Content="查询"
                                            Style="{StaticResource ButtonPrimary}" />
                                    </Grid>
                                </hc:Card>

    -->
    <!--  日志列表  -->
    <!--
                                <DataGrid
                                    Grid.Row="1"
                                    AutoGenerateColumns="False"
                                    CanUserAddRows="False"
                                    ItemsSource="{Binding OperationLogs}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn
                                            Width="150"
                                            Binding="{Binding DateTime}"
                                            Header="时间" />
                                        <DataGridTextColumn
                                            Width="100"
                                            Binding="{Binding Username}"
                                            Header="用户" />
                                        <DataGridTextColumn
                                            Width="100"
                                            Binding="{Binding Action}"
                                            Header="操作类型" />
                                        <DataGridTextColumn
                                            Width="*"
                                            Binding="{Binding Description}"
                                            Header="详细描述" />
                                        <DataGridTextColumn
                                            Width="120"
                                            Binding="{Binding IpAddress}"
                                            Header="IP地址" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </TabItem>
                    </TabControl>
                </Grid>
            </Grid>
        </Border>

    -->
    <!--  加载遮罩  -->
    <!--  <Grid Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"  -->
    <!--
        <Grid Background="#80000000" Visibility="Hidden">
            <hc:LoadingCircle
                Width="50"
                Height="50"
                DotCount="5"
                DotInterval="10"
                DotSpeed="4"
                IsRunning="True" />
        </Grid>
    </Grid>-->
</Page>
