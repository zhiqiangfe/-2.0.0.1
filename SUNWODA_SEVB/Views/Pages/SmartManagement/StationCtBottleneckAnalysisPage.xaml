<Page
    x:Class="SUNWODA_SEVB.Views.Pages.SmartManagement.StationCtBottleneckAnalysisPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:SUNWODA_SEVB.ViewModels.Pages.SmartManagement"
    d:DataContext="{d:DesignInstance Type=vm:VM_StationCtBottleneckAnalysisPage}"
    d:DesignHeight="900"
    d:DesignWidth="1600"
    Background="#F5F7FA"
    mc:Ignorable="d">
    <Page.Resources>
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D7E1EF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
        </Style>
        <Style x:Key="MutedText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#637083" />
            <Setter Property="FontSize" Value="13" />
        </Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#172033" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="36" />
            <Setter Property="MinHeight" Value="36" />
            <Setter Property="Padding" Value="8,-1,8,0" />
            <Setter Property="Foreground" Value="#1F2937" />
            <Setter Property="Background" Value="#EEF3FA" />
            <Setter Property="BorderBrush" Value="#CCD6E3" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="20,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </Page.Resources>

    <Grid Margin="24,14,24,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="60" />
                <RowDefinition Height="74" />
                <RowDefinition Height="182" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid>
                <StackPanel>
                    <TextBlock FontSize="23" FontWeight="Bold" Foreground="#172033" LineHeight="29" Text="工位 CT 与动作节拍瓶颈分析" />
                    <TextBlock Margin="0,2,0,0" FontSize="13" Foreground="#526174" LineHeight="18" Text="以工位流、动作甘特和节拍平衡图定位瓶颈" />
                </StackPanel>
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Margin="0,0,10,0" Padding="14,7" Background="#EEF3FA" BorderBrush="#CAD5E4" BorderThickness="1" CornerRadius="14">
                        <TextBlock Foreground="#344054" Text="{Binding SelectedLine}" />
                    </Border>
                    <Border Margin="0,0,10,0" Padding="14,7" Background="#EEF3FA" BorderBrush="#CAD5E4" BorderThickness="1" CornerRadius="14">
                        <TextBlock Foreground="#344054" Text="{Binding SelectedShift}" />
                    </Border>
                    <Button Background="#2F66F6" BorderBrush="#2F66F6" Command="{Binding RefreshCommand}" Content="刷新" Foreground="White" />
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Margin="0,8,0,0" Padding="16,8" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.1*" />
                        <ColumnDefinition Width="1.2*" />
                        <ColumnDefinition Width=".85*" />
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Margin="0,0,18,0">
                        <TextBlock Style="{StaticResource MutedText}" Text="产线" />
                        <ComboBox Margin="0,3,0,0" ItemsSource="{Binding Lines}" SelectedItem="{Binding SelectedLine}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="0,0,18,0">
                        <TextBlock Style="{StaticResource MutedText}" Text="工位组" />
                        <ComboBox Margin="0,3,0,0" ItemsSource="{Binding StationGroups}" SelectedItem="{Binding SelectedStationGroup}" />
                    </StackPanel>
                    <StackPanel Grid.Column="2" Margin="0,0,18,0">
                        <TextBlock Style="{StaticResource MutedText}" Text="班次" />
                        <ComboBox Margin="0,3,0,0" ItemsSource="{Binding Shifts}" SelectedItem="{Binding SelectedShift}" />
                    </StackPanel>
                    <StackPanel Grid.Column="3" Margin="0,0,18,0">
                        <TextBlock Style="{StaticResource MutedText}" Text="时间范围" />
                        <TextBox Height="36" Margin="0,3,0,0" Padding="10,0" Background="#EEF3FA" BorderBrush="#CCD6E3" Foreground="#1F2937" IsReadOnly="True" Text="{Binding TimeRange, Mode=OneWay}" VerticalContentAlignment="Center" />
                    </StackPanel>
                    <Button Grid.Column="4" Margin="0,17,0,0" Background="#EEF3FA" BorderBrush="#D7E1EF" Content="导出" Foreground="#46566A" />
                </Grid>
            </Border>

            <Border Grid.Row="2" Margin="0,12,0,0" Padding="20,12" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}" Text="产线工位流与瓶颈热力" />
                        <TextBlock Margin="0,4,0,0" Style="{StaticResource MutedText}" Text="每个工位块展示 CT、等待、异常状态；红色越深代表越接近或超过节拍上限" />
                    </StackPanel>
                    <ItemsControl Grid.Row="1" Margin="0,14,0,0" ItemsSource="{Binding Stations}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="6" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,18,0">
                                    <Border Height="88" Background="White" BorderBrush="{Binding AccentBrush}" BorderThickness="2" CornerRadius="9">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="8" />
                                                <RowDefinition Height="*" />
                                            </Grid.RowDefinitions>
                                            <Border Background="{Binding AccentBrush}" CornerRadius="7,7,0,0" />
                                            <Grid Grid.Row="1" Margin="12,8,12,10">
                                                <TextBlock FontSize="14" FontWeight="SemiBold" Foreground="#172033" Text="{Binding Name}" />
                                                <Border HorizontalAlignment="Right" Padding="10,3" Background="{Binding AccentBrush}" CornerRadius="12">
                                                    <TextBlock FontSize="11" Foreground="White" Text="{Binding State}" />
                                                </Border>
                                                <TextBlock Margin="0,28,0,0" FontSize="23" FontWeight="Bold" Foreground="{Binding AccentBrush}" Text="{Binding Ct}" />
                                                <TextBlock Margin="88,37,0,0" FontSize="12" Foreground="#637083" Text="{Binding WaitText}" />
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>

            <Grid Grid.Row="3" Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.1*" />
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="1.15*" />
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <Border Padding="18,12" Style="{StaticResource PanelStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitle}" Text="节拍平衡图" />
                            <TextBlock Margin="0,4,0,0" Style="{StaticResource MutedText}" Text="目标 CT 4.50s，超过红线的工位形成节拍约束" />
                        </StackPanel>
                        <Grid Grid.Row="1" Margin="0,12,0,0">
                            <Line X1="286" X2="286" Y1="0" Y2="300" Stroke="#E94855" StrokeThickness="2" />
                            <TextBlock Margin="294,0,0,0" Foreground="#E94855" Text="目标 4.50s" />
                            <ItemsControl Margin="0,24,0,0" ItemsSource="{Binding Stations}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Height="44">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="112" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock VerticalAlignment="Center" Foreground="#344054" Text="{Binding Name}" />
                                            <Grid Grid.Column="1" VerticalAlignment="Center">
                                                <Border Height="24" Background="#EAF0F8" CornerRadius="4" />
                                                <Border Width="{Binding BarWidth}" Height="24" HorizontalAlignment="Left" Background="{Binding AccentBrush}" CornerRadius="4" />
                                                <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="White" Text="{Binding Ct}" />
                                            </Grid>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Grid>
                </Border>

                <Border Grid.Column="2" Padding="18,12" Style="{StaticResource PanelStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitle}" Text="瓶颈工位动作甘特" />
                            <TextBlock Margin="0,4,0,0" Style="{StaticResource MutedText}" Text="激光焊接单件动作序列，识别等待和设备动作本体耗时" />
                        </StackPanel>
                        <Grid Grid.Row="1" Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="94" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Canvas Grid.Column="1" Margin="0,4,0,0">
                                <Line X1="0" X2="0" Y1="0" Y2="300" Stroke="#E6ECF4" />
                                <Line X1="100" X2="100" Y1="0" Y2="300" Stroke="#E6ECF4" />
                                <Line X1="200" X2="200" Y1="0" Y2="300" Stroke="#E6ECF4" />
                                <Line X1="300" X2="300" Y1="0" Y2="300" Stroke="#E6ECF4" />
                                <Line X1="400" X2="400" Y1="0" Y2="300" Stroke="#E6ECF4" />
                                <TextBlock Canvas.Left="0" Canvas.Top="0" FontSize="11" Foreground="#637083" Text="0s" />
                                <TextBlock Canvas.Left="96" Canvas.Top="0" FontSize="11" Foreground="#637083" Text="1s" />
                                <TextBlock Canvas.Left="196" Canvas.Top="0" FontSize="11" Foreground="#637083" Text="2s" />
                                <TextBlock Canvas.Left="296" Canvas.Top="0" FontSize="11" Foreground="#637083" Text="3s" />
                                <TextBlock Canvas.Left="396" Canvas.Top="0" FontSize="11" Foreground="#637083" Text="4s" />
                            </Canvas>
                            <ItemsControl Margin="0,24,0,0" ItemsSource="{Binding ActionSteps}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Height="44">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="94" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock VerticalAlignment="Center" Foreground="#344054" Text="{Binding Name}" />
                                            <Canvas Grid.Column="1">
                                                <Border Canvas.Left="{Binding Left}" Width="{Binding Width}" Height="24" Background="{Binding FillBrush}" CornerRadius="5">
                                                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12" Foreground="White" Text="{Binding Duration}" />
                                                </Border>
                                            </Canvas>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Margin="160,304,0,0" Foreground="#E94855" Text="关键损失区：等待握手 + 焊接动作" />
                        </Grid>
                    </Grid>
                </Border>

                <Border Grid.Column="4" Padding="18,12" Style="{StaticResource PanelStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}" Text="瓶颈诊断卡" />
                        <TextBlock Margin="0,4,0,10" Style="{StaticResource MutedText}" Text="以规则引擎输出可执行定位结论" />
                        <Border Padding="16,12" Background="#FFF1F1" BorderBrush="#F8B4B4" BorderThickness="1" CornerRadius="8">
                            <Grid>
                                <StackPanel>
                                    <TextBlock Style="{StaticResource MutedText}" Text="当前瓶颈" />
                                    <TextBlock Margin="0,6,0,0" FontSize="23" FontWeight="Bold" Foreground="#E94855" Text="{Binding BottleneckStation}" />
                                </StackPanel>
                                <TextBlock HorizontalAlignment="Right" VerticalAlignment="Bottom" FontSize="23" FontWeight="Bold" Foreground="#E94855" Text="{Binding BottleneckCt}" />
                            </Grid>
                        </Border>
                        <TextBlock Margin="0,10,0,8" Style="{StaticResource MutedText}" Text="{Binding BottleneckSummary}" TextWrapping="Wrap" />
                        <ItemsControl ItemsSource="{Binding CauseTags}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,8,8" Padding="12,6" Background="{Binding FillBrush}" CornerRadius="14">
                                        <TextBlock FontSize="12" Foreground="White" Text="{Binding Text}" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <ItemsControl Margin="0,10,0,0" ItemsSource="{Binding Recommendations}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="32" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Border Width="24" Height="24" Background="#2F66F6" CornerRadius="12">
                                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12" Foreground="White" Text="{Binding No}" />
                                        </Border>
                                        <TextBlock Grid.Column="1" Foreground="#344054" LineHeight="22" Text="{Binding Text}" TextWrapping="Wrap" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>
    </Grid>
</Page>
