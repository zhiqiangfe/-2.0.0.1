<Page
    x:Class="SUNWODA_SEVB.Views.Pages.SmartManagement.ProductQualityTracePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="900"
    d:DesignWidth="1600"
    Background="#F6F8FB"
    mc:Ignorable="d">
    <Page.Resources>
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D9E3F0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
        </Style>
        <Style x:Key="MutedText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#637083" />
            <Setter Property="FontSize" Value="12" />
        </Style>
        <Style x:Key="TitleText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#172033" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="8,0" />
            <Setter Property="Background" Value="#F0F4FA" />
            <Setter Property="BorderBrush" Value="#CAD5E4" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </Page.Resources>

    <Grid Margin="24,14,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="72" />
            <RowDefinition Height="70" />
            <RowDefinition Height="76" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Padding="22,10" Style="{StaticResource PanelStyle}">
            <Grid>
                <StackPanel>
                    <TextBlock FontSize="24" Style="{StaticResource TitleText}" LineHeight="30" Text="产品品质追溯 - 数据查询" />
                    <TextBlock Margin="0,2,0,0" FontSize="13" Foreground="#637083" LineHeight="18" Text="按产品码 / 批次 / 工序检索全链路数据，后续对接数据库分页查询" />
                </StackPanel>
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Width="84" Margin="0,0,8,0" Background="#2F66F6" BorderBrush="#2F66F6" Command="{Binding SearchCommand}" Content="查询" Foreground="White" />
                    <Button Width="84" Margin="0,0,8,0" Background="#EEF3FA" BorderBrush="#CAD5E4" Command="{Binding ExportCommand}" Content="导出" Foreground="#344054" />
                    <Button Width="70" Background="#EEF3FA" BorderBrush="#CAD5E4" Content="重置" Foreground="#344054" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="0,8,0,0" Padding="14,7" Style="{StaticResource PanelStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.32*" />
                    <ColumnDefinition Width=".9*" />
                    <ColumnDefinition Width=".9*" />
                    <ColumnDefinition Width=".72*" />
                    <ColumnDefinition Width=".95*" />
                    <ColumnDefinition Width=".95*" />
                </Grid.ColumnDefinitions>
                <StackPanel Margin="0,0,14,0">
                    <TextBlock Style="{StaticResource MutedText}" Text="产品编号" />
                    <TextBox Margin="0,2,0,0" Text="{Binding ProductCode}" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0,0,14,0">
                    <TextBlock Style="{StaticResource MutedText}" Text="批次" />
                    <TextBox Margin="0,2,0,0" Text="{Binding BatchNo}" />
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="0,0,14,0">
                    <TextBlock Style="{StaticResource MutedText}" Text="工序" />
                    <TextBox Margin="0,2,0,0" Text="{Binding ProcessName}" />
                </StackPanel>
                <StackPanel Grid.Column="3" Margin="0,0,14,0">
                    <TextBlock Style="{StaticResource MutedText}" Text="视觉结果" />
                    <TextBox Margin="0,2,0,0" Text="{Binding VisualResult}" />
                </StackPanel>
                <StackPanel Grid.Column="4" Margin="0,0,14,0">
                    <TextBlock Style="{StaticResource MutedText}" Text="开始时间" />
                    <TextBox Margin="0,2,0,0" Text="{Binding StartTime}" />
                </StackPanel>
                <StackPanel Grid.Column="5">
                    <TextBlock Style="{StaticResource MutedText}" Text="结束时间" />
                    <TextBox Margin="0,2,0,0" Text="{Binding EndTime}" />
                </StackPanel>
            </Grid>
        </Border>

        <ItemsControl Grid.Row="2" Margin="0,10,0,0" ItemsSource="{Binding SummaryItems}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,12,0" Padding="14,9" Style="{StaticResource PanelStyle}">
                        <Grid>
                            <Border Width="5" HorizontalAlignment="Left" Background="{Binding Color}" />
                            <StackPanel Margin="16,0,0,0">
                                <TextBlock Style="{StaticResource MutedText}" Text="{Binding Title}" />
                                <TextBlock Margin="0,6,0,0" FontSize="20" FontWeight="Bold" Foreground="#172033" Text="{Binding Value}" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.92*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Border Padding="18,12" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="38" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="18" Style="{StaticResource TitleText}" Text="追溯数据列表" />
                        <TextBlock Margin="16,4,0,0" Style="{StaticResource MutedText}" Text="只展示摘要字段，点击行后右侧查看详情" />
                    </StackPanel>
                    <DataGrid
                        Grid.Row="1"
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        ColumnHeaderHeight="30"
                        EnableRowVirtualization="True"
                        IsReadOnly="True"
                        ItemsSource="{Binding Records}"
                        RowHeight="30"
                        SelectedItem="{Binding SelectedRecord, Mode=TwoWay}"
                        SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="150" Binding="{Binding ProductCode}" Header="产品编号" />
                            <DataGridTextColumn Width="82" Binding="{Binding InTime}" Header="进站时间" />
                            <DataGridTextColumn Width="82" Binding="{Binding OutTime}" Header="出站时间" />
                            <DataGridTextColumn Width="100" Binding="{Binding Station}" Header="工位" />
                            <DataGridTextColumn Width="68" Binding="{Binding VisualResult}" Header="视觉" />
                            <DataGridTextColumn Width="68" Binding="{Binding InResult}" Header="进站" />
                            <DataGridTextColumn Width="68" Binding="{Binding OutResult}" Header="出站" />
                            <DataGridTextColumn Width="*" MinWidth="160" Binding="{Binding KeyParameter}" Header="关键参数" />
                            <DataGridTextColumn Width="70" Binding="{Binding BatchNo}" Header="批次" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="2" Padding="18,14" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="92" />
                        <RowDefinition Height="78" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="56" />
                    </Grid.RowDefinitions>
                    <Border Padding="16,12" Background="#F7FAFC" BorderBrush="#DCE6F2" BorderThickness="1" CornerRadius="8">
                        <StackPanel>
                            <TextBlock Style="{StaticResource MutedText}" Text="当前选中产品" />
                            <TextBlock Margin="0,8,0,0" FontSize="19" FontWeight="Bold" Foreground="#172033" Text="{Binding SelectedRecord.ProductCode}" TextTrimming="CharacterEllipsis" />
                            <TextBlock Margin="0,6,0,0" Foreground="#E94855" Text="{Binding SelectedRecord.VisualResult}" />
                        </StackPanel>
                    </Border>

                    <ItemsControl Grid.Row="1" Margin="0,14,0,0" ItemsSource="{Binding TraceSteps}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="5" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Height="44" Margin="0,0,8,0" Background="White" BorderBrush="{Binding Color}" BorderThickness="2" CornerRadius="6">
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock HorizontalAlignment="Center" FontSize="12" Foreground="#243044" Text="{Binding Name}" />
                                        <TextBlock HorizontalAlignment="Center" FontSize="11" Foreground="{Binding Color}" Text="{Binding State}" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <ItemsControl Grid.Row="2" Margin="0,12,0,0" ItemsSource="{Binding DetailItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Height="38">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="92" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock VerticalAlignment="Center" Style="{StaticResource MutedText}" Text="{Binding Name}" />
                                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Foreground="#243044" Text="{Binding Value}" TextTrimming="CharacterEllipsis" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Border Grid.Row="3" Padding="12,8" Background="#FFF7E8" BorderBrush="#F3DFC0" BorderThickness="1" CornerRadius="8">
                        <TextBlock FontSize="12" Foreground="#7A4A00" Text="建议：追溯页只查数据库摘要，详情按产品码二次查询，避免一次加载大表。" TextWrapping="Wrap" />
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
