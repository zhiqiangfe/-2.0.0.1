<Page
    x:Class="HTHIUM.Views.Pages.SmartManagement.HmiAlarmTopAnalysisPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="900"
    d:DesignWidth="1600"
    Background="#F3F6FA"
    mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008">
    <Page.Resources>
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D7E1EF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
        </Style>
        <Style x:Key="MutedText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#637083" />
            <Setter Property="FontSize" Value="13" />
        </Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#172033" />
            <Setter Property="FontSize" Value="19" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="FlatAlarmButton" TargetType="Button">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,0,0,9" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="66" />
            <RowDefinition Height="62" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <StackPanel>
                <TextBlock FontSize="24" FontWeight="Bold" Foreground="#172033" LineHeight="30" Text="HMI 报警记录与报警 Top 分析" />
                <TextBlock Margin="0,2,0,0" FontSize="13" Foreground="#637083" LineHeight="18" Text="报警复盘、频次排名、热力定位和闭环处理工作台" />
            </StackPanel>
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Margin="0,0,8,0" Padding="12,6" Background="#EEF3FA" BorderBrush="#CAD5E4" BorderThickness="1" CornerRadius="13">
                    <TextBlock Foreground="#344054" Text="{Binding SelectedLine}" />
                </Border>
                <Border Margin="0,0,8,0" Padding="12,6" Background="#EEF3FA" BorderBrush="#CAD5E4" BorderThickness="1" CornerRadius="13">
                    <TextBlock Foreground="#344054" Text="{Binding SelectedDevice}" />
                </Border>
                <Border Margin="0,0,8,0" Padding="12,6" Background="#EEF3FA" BorderBrush="#CAD5E4" BorderThickness="1" CornerRadius="13">
                    <TextBlock Foreground="#344054" Text="{Binding SelectedShift}" />
                </Border>
                <hc:DateTimePicker
                    Width="168"
                    Height="34"
                    Margin="0,0,8,0"
                    Background="#EEF3FA"
                    BorderBrush="#CAD5E4"
                    DateTimeFormat="yyyy-MM-dd HH:mm"
                    SelectedDateTime="{Binding StartDateTime}" />
                <TextBlock Margin="0,0,8,0" VerticalAlignment="Center" Foreground="#637083" Text="-" />
                <hc:DateTimePicker
                    Width="168"
                    Height="34"
                    Margin="0,0,12,0"
                    Background="#EEF3FA"
                    BorderBrush="#CAD5E4"
                    DateTimeFormat="yyyy-MM-dd HH:mm"
                    SelectedDateTime="{Binding EndDateTime}" />
                <Button Width="80" Height="34" Background="#2F66F6" BorderBrush="#2F66F6" Command="{Binding RefreshCommand}" Content="查询" Foreground="White" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Margin="0,6,0,0" Background="#172033" CornerRadius="8">
            <ItemsControl Margin="20,6" ItemsSource="{Binding SummaryItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="5" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,18,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="112" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="17" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBlock FontSize="12" Foreground="#B7C4D6" Text="{Binding Name}" />
                                <TextBlock Grid.Row="1" FontSize="24" FontWeight="Bold" Foreground="White" Text="{Binding Value}" />
                            </Grid>
                            <TextBlock
                                Grid.Column="1"
                                Margin="8,18,0,0"
                                FontSize="12"
                                Foreground="#9FB0C7"
                                Text="{Binding Description}"
                                TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <Grid Grid.Row="2" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.02*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="1.55*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="1.14*" />
            </Grid.ColumnDefinitions>

            <Border Padding="20,16" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}" Text="报警事件流" />
                        <TextBlock Margin="0,5,0,0" Style="{StaticResource MutedText}" Text="按时间倒序展示，颜色代表报警优先级" />
                    </StackPanel>
                    <ScrollViewer Grid.Row="1" Margin="0,16,0,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding AlarmEvents}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button
                                        Command="{Binding DataContext.SelectAlarmCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource FlatAlarmButton}">
                                        <Border Height="68" Background="{Binding Background}" BorderBrush="{Binding BorderColor}" BorderThickness="1" CornerRadius="6">
                                            <Grid>
                                                <Border Width="6" HorizontalAlignment="Left" Background="{Binding AccentColor}" CornerRadius="6,0,0,6" />
                                                <Border Width="30" Height="24" Margin="18,11,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Background="{Binding AccentColor}" CornerRadius="12">
                                                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11" Foreground="White" Text="{Binding Level}" />
                                                </Border>
                                                <TextBlock Margin="60,9,8,0" FontSize="15" FontWeight="SemiBold" Foreground="#243044" Text="{Binding Name}" TextTrimming="CharacterEllipsis" />
                                                <TextBlock Margin="60,37,0,0" Foreground="#637083">
                                                    <Run Text="{Binding Time}" />
                                                    <Run Text="  |  " />
                                                    <Run Text="{Binding Station}" />
                                                </TextBlock>
                                                <TextBlock Margin="0,37,16,0" HorizontalAlignment="Right" Foreground="{Binding AccentColor}" Text="{Binding Duration}" />
                                            </Grid>
                                        </Border>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="18" />
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>

                <Border Padding="20,16" Style="{StaticResource PanelStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitle}" Text="报警 Top 排名" />
                            <TextBlock Margin="0,5,0,0" Style="{StaticResource MutedText}" Text="按报警名称统计频次，优先治理高频和长时报警" />
                        </StackPanel>
                        <ScrollViewer Grid.Row="1" Margin="0,16,0,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <ItemsControl ItemsSource="{Binding AlarmTopItems}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Height="36">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="32" />
                                                <ColumnDefinition Width="168" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="58" />
                                                <ColumnDefinition Width="58" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock VerticalAlignment="Center" Foreground="#637083" Text="{Binding Rank}" />
                                            <TextBlock Grid.Column="1" VerticalAlignment="Center" Foreground="#243044" Text="{Binding Name}" TextTrimming="CharacterEllipsis" />
                                            <Grid Grid.Column="2" VerticalAlignment="Center">
                                                <ProgressBar Height="22" Maximum="100" Value="{Binding Percent, Mode=OneWay}" Foreground="{Binding FillColor}" Background="#EAF0F8" BorderBrush="#EAF0F8" />
                                            </Grid>
                                            <TextBlock Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{Binding FillColor}" Text="{Binding Count}" />
                                            <TextBlock Grid.Column="4" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="#637083" Text="{Binding PercentText}" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <Border Grid.Row="2" Padding="20,16" Style="{StaticResource PanelStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitle}" Text="报警时段 x 工位热力矩阵" />
                            <TextBlock Margin="0,5,0,0" Style="{StaticResource MutedText}" Text="定位集中爆发时段和异常工位组" />
                        </StackPanel>
                        <Grid Grid.Row="1" Margin="0,16,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="54" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <UniformGrid Grid.Column="1" Columns="7" Height="18" VerticalAlignment="Top">
                                <TextBlock FontSize="11" Foreground="#637083" Text="08:00" TextAlignment="Center" />
                                <TextBlock FontSize="11" Foreground="#637083" Text="10:00" TextAlignment="Center" />
                                <TextBlock FontSize="11" Foreground="#637083" Text="12:00" TextAlignment="Center" />
                                <TextBlock FontSize="11" Foreground="#637083" Text="14:00" TextAlignment="Center" />
                                <TextBlock FontSize="11" Foreground="#637083" Text="16:00" TextAlignment="Center" />
                                <TextBlock FontSize="11" Foreground="#637083" Text="18:00" TextAlignment="Center" />
                                <TextBlock FontSize="11" Foreground="#637083" Text="20:00" TextAlignment="Center" />
                            </UniformGrid>
                            <ItemsControl Grid.ColumnSpan="2" Margin="0,24,0,0" ItemsSource="{Binding HeatRows}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Height="31">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="54" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock VerticalAlignment="Center" Foreground="#243044" Text="{Binding Station}" />
                                            <ItemsControl Grid.Column="1" ItemsSource="{Binding Cells}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <UniformGrid Columns="7" />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Height="23" Margin="4,0" Background="{Binding FillColor}" CornerRadius="4">
                                                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11" Foreground="{Binding TextColor}" Text="{Binding Value}" />
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Column="4" Padding="20,16" Style="{StaticResource PanelStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource SectionTitle}" Text="报警详情与处理闭环" />
                    <TextBlock Margin="0,5,0,16" Style="{StaticResource MutedText}" Text="选中报警后的证据、影响与处理进度" />
                    <Border Padding="18,14" Background="#FFF1F1" BorderBrush="#F8B4B4" BorderThickness="1" CornerRadius="8">
                        <StackPanel>
                            <TextBlock Style="{StaticResource MutedText}" Text="当前选中" />
                            <TextBlock Margin="0,7,0,0" FontSize="24" FontWeight="Bold" Foreground="#E94855" Text="{Binding SelectedAlarmName}" />
                            <TextBlock Margin="0,8,0,0" Foreground="#7A3E3E" Text="{Binding SelectedAlarmDetail}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                    <ItemsControl Margin="0,16,0,0" ItemsSource="{Binding EvidenceItems}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="2" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,0,12,9">
                                    <TextBlock FontSize="12" Foreground="#637083" Text="{Binding Name}" />
                                    <Border Height="28" Margin="0,4,0,0" Padding="10,0" Background="#EEF3FA" BorderBrush="#CAD5E4" BorderThickness="1" CornerRadius="4">
                                        <TextBlock VerticalAlignment="Center" FontSize="12" Foreground="#243044" Text="{Binding Value}" />
                                    </Border>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Margin="0,10,0,10" FontSize="16" FontWeight="SemiBold" Foreground="#172033" Text="闭环处理时间线" />
                    <ItemsControl ItemsSource="{Binding ProcessSteps}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Height="43">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="26" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="64" />
                                        <ColumnDefinition Width="64" />
                                    </Grid.ColumnDefinitions>
                                    <Ellipse Width="16" Height="16" Fill="{Binding FillColor}" />
                                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Foreground="#243044" Text="{Binding Name}" />
                                    <TextBlock Grid.Column="2" VerticalAlignment="Center" Foreground="#637083" Text="{Binding Time}" />
                                    <TextBlock Grid.Column="3" VerticalAlignment="Center" Foreground="{Binding FillColor}" Text="{Binding State}" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <Border Margin="0,10,0,0" Padding="14,10" Background="#EEF6FF" BorderBrush="#C7DDFF" BorderThickness="1" CornerRadius="6">
                        <TextBlock Foreground="#24508F" LineHeight="22" Text="{Binding Recommendation}" TextWrapping="Wrap" />
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
