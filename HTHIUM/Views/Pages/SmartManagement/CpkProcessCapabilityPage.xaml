<Page
    x:Class="HTHIUM.Views.Pages.SmartManagement.CpkProcessCapabilityPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="900"
    d:DesignWidth="1600"
    Background="#F6F8FB"
    mc:Ignorable="d">
    <Page.Resources>
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D9E3F0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
        </Style>
        <Style x:Key="MutedText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#637083" />
            <Setter Property="FontSize" Value="12" />
        </Style>
        <Style x:Key="TitleText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#172033" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="8,0" />
            <Setter Property="Background" Value="#F0F4FA" />
            <Setter Property="BorderBrush" Value="#CAD5E4" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </Page.Resources>

    <Grid Margin="24,14,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="72" />
            <RowDefinition Height="70" />
            <RowDefinition Height="76" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Padding="22,10" Style="{StaticResource PanelStyle}">
            <Grid>
                <StackPanel>
                    <TextBlock FontSize="24" Style="{StaticResource TitleText}" LineHeight="30" Text="CPK 计算与过程能力分析" />
                    <TextBlock Margin="0,2,0,0" FontSize="13" Foreground="#637083" LineHeight="18" Text="按工位 / 参数 / 时间范围独立计算 CPK，支持多小图对比和异常定位" />
                </StackPanel>
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Width="84" Margin="0,0,8,0" Background="#2F66F6" BorderBrush="#2F66F6" Command="{Binding RefreshCommand}" Content="查询" Foreground="White" />
                    <Button Width="84" Margin="0,0,8,0" Background="#EEF3FA" BorderBrush="#CAD5E4" Command="{Binding ExportCommand}" Content="导出" Foreground="#344054" />
                    <Button Width="70" Background="#EEF3FA" BorderBrush="#CAD5E4" Content="重置" Foreground="#344054" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="0,8,0,0" Padding="14,7" Style="{StaticResource PanelStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width=".55*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width=".95*" />
                    <ColumnDefinition Width=".9*" />
                    <ColumnDefinition Width="1.05*" />
                    <ColumnDefinition Width="1.05*" />
                </Grid.ColumnDefinitions>
                <StackPanel Margin="0,0,14,0">
                    <TextBlock Style="{StaticResource MutedText}" Text="产线" />
                    <TextBox Margin="0,2,0,0" Text="{Binding LineName}" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0,0,14,0">
                    <TextBlock Style="{StaticResource MutedText}" Text="工序" />
                    <TextBox Margin="0,2,0,0" Text="{Binding ProcessName}" />
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="0,0,14,0">
                    <TextBlock Style="{StaticResource MutedText}" Text="参数" />
                    <TextBox Margin="0,2,0,0" Text="{Binding ParameterName}" />
                </StackPanel>
                <StackPanel Grid.Column="3" Margin="0,0,14,0">
                    <TextBlock Style="{StaticResource MutedText}" Text="样本" />
                    <TextBox Margin="0,2,0,0" Text="{Binding SampleCount}" />
                </StackPanel>
                <StackPanel Grid.Column="4" Margin="0,0,14,0">
                    <TextBlock Style="{StaticResource MutedText}" Text="开始时间" />
                    <TextBox Margin="0,2,0,0" Text="{Binding StartTime}" />
                </StackPanel>
                <StackPanel Grid.Column="5">
                    <TextBlock Style="{StaticResource MutedText}" Text="结束时间" />
                    <TextBox Margin="0,2,0,0" Text="{Binding EndTime}" />
                </StackPanel>
            </Grid>
        </Border>

        <ItemsControl Grid.Row="2" Margin="0,10,0,0" ItemsSource="{Binding SummaryItems}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,12,0" Padding="14,9" Style="{StaticResource PanelStyle}">
                        <Grid>
                            <Border Width="5" HorizontalAlignment="Left" Background="{Binding Color}" />
                            <StackPanel Margin="16,0,0,0">
                                <TextBlock Style="{StaticResource MutedText}" Text="{Binding Title}" />
                                <StackPanel Margin="0,6,0,0" Orientation="Horizontal">
                                    <TextBlock FontSize="20" FontWeight="Bold" Foreground="#172033" Text="{Binding Value}" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Margin="14,5,0,0" FontSize="12" Foreground="{Binding Color}" Text="{Binding Description}" TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.25*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Border Padding="18,12" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="38" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="18" Style="{StaticResource TitleText}" Text="多工位 CPK 小图矩阵" />
                        <TextBlock Margin="18,4,0,0" Style="{StaticResource MutedText}" Text="异常工位红色高亮，适合一屏快速定位过程能力不足点" />
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding MiniCharts}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="5" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Height="132" Margin="0,0,12,12" Padding="10,8" Background="#FBFCFE" BorderBrush="{Binding Color}" BorderThickness="1" CornerRadius="6">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="24" />
                                                <RowDefinition Height="*" />
                                                <RowDefinition Height="20" />
                                            </Grid.RowDefinitions>
                                            <Grid>
                                                <TextBlock FontSize="12" Foreground="#172033" Text="{Binding Name}" TextTrimming="CharacterEllipsis" />
                                                <TextBlock HorizontalAlignment="Right" FontSize="12" FontWeight="SemiBold" Foreground="{Binding Color}" Text="{Binding CpkText}" />
                                            </Grid>
                                            <Grid Grid.Row="1" Margin="4,4,4,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="*" />
                                                    <RowDefinition Height="1" />
                                                </Grid.RowDefinitions>
                                                <Line X1="0" X2="168" Y1="18" Y2="18" Stroke="#E94855" StrokeDashArray="4 4" />
                                                <Line X1="0" X2="168" Y1="43" Y2="43" Stroke="#2F66F6" StrokeDashArray="4 4" />
                                                <Polyline Points="{Binding TrendPoints}" Stroke="{Binding Color}" StrokeThickness="2.4" StrokeLineJoin="Round" />
                                            </Grid>
                                            <TextBlock Grid.Row="2" HorizontalAlignment="Right" FontSize="11" Foreground="{Binding Color}" Text="{Binding State}" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <Border Grid.Column="2" Padding="18,14" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="132" />
                        <RowDefinition Height="170" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Padding="16,12" Background="#F7FAFC" BorderBrush="#DCE6F2" BorderThickness="1" CornerRadius="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <ItemsControl ItemsSource="{Binding SpecItems}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Height="22">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="92" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock FontSize="12" Foreground="#637083" Text="{Binding Name}" />
                                            <TextBlock Grid.Column="1" FontSize="12" Foreground="{Binding Color}" Text="{Binding Value}" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>

                    <Border Grid.Row="1" Margin="0,14,0,0" Padding="16,12" Background="#FBFCFE" BorderBrush="#DCE6F2" BorderThickness="1" CornerRadius="8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="24" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock FontSize="16" Style="{StaticResource TitleText}" Text="当前参数分布" />
                            <Grid Grid.Row="1" Margin="0,8,0,0">
                                <ItemsControl VerticalAlignment="Bottom" ItemsSource="{Binding HistogramBars}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Width="18" Height="{Binding Height}" Margin="4,0" VerticalAlignment="Bottom" Background="#72A3FF" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <Line X1="64" X2="64" Y1="8" Y2="118" Stroke="#E94855" StrokeDashArray="5 4" StrokeThickness="2" />
                                <Line X1="172" X2="172" Y1="8" Y2="118" Stroke="#2F66F6" StrokeDashArray="5 4" StrokeThickness="2" />
                                <Line X1="286" X2="286" Y1="8" Y2="118" Stroke="#E94855" StrokeDashArray="5 4" StrokeThickness="2" />
                            </Grid>
                        </Grid>
                    </Border>

                    <Border Grid.Row="2" Margin="0,14,0,0" Padding="16,12" Background="#FFF7E8" BorderBrush="#F3DFC0" BorderThickness="1" CornerRadius="8">
                        <StackPanel>
                            <TextBlock FontSize="16" Foreground="#7A4A00" FontWeight="SemiBold" Text="统计结论" />
                            <ItemsControl Margin="0,12,0,0" ItemsSource="{Binding Conclusions}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Height="34">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="26" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Border Width="18" Height="18" Background="#F59E0B" CornerRadius="9">
                                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11" Foreground="White" Text="{Binding Index}" />
                                            </Border>
                                            <TextBlock Grid.Column="1" FontSize="13" Foreground="#7A4A00" Text="{Binding Text}" TextWrapping="Wrap" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
