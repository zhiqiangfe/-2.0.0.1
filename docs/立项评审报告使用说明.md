# 设备智慧系统立项评审报告

文件：`设备智慧系统开发项目-立项评审报告.pptx.bin`

该文件是未加密的标准 PowerPoint 文件。为避免企业终端加密程序按 `.pptx` 扩展名自动加密，仓库中使用 `.pptx.bin` 扩展名保存。

使用方法：

1. 从 GitHub 下载文件。
2. 将文件名后缀由 `.pptx.bin` 修改为 `.pptx`。
3. 使用 Microsoft PowerPoint 打开。

如果在公司受控电脑上改名，终端加密软件仍可能自动对文件加密；GitHub 仓库中保存的原始版本不受影响。
